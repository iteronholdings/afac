원고 분량 회귀 수정: 달성 가능 상한 클램프 + 수동 경로 분량 검수 + 테스트 정상화

Codex 지적 대응(6d5984b 결함):
- 규칙 기반 생성기가 못 채우는 큰 목표(1000/3000자)가 flagged 원고로 배정되던 문제.
  전 카테고리(뷰티·펫·베이비 등 작은 문장풀 포함) 검증 상한을 사진 700·글자 400으로 확정.
- reviewSupervisor.clampTargetChars: 목표를 [50, 상한]으로 정규화해 생성·검수가 동일 값 사용.
  superviseGenerated/Manual 모두 진입부에서 클램프 → 1000/3000 요청도 flagged 0.
- 수동 수정(updateReviewDraft): campaign 목표(photo/textDraftChars)를 supervise에 전달,
  superviseManualDraft가 "목표 분량 이탈"도 경고에 포함(Codex 지적한 누락 2건 해소).
- 스키마·마법사 입력 상한을 700/400으로, 안내에 상한 표기. CampaignWizard clipboard 이터레이션 tsc 수정.

테스트: reviewSupervisor.test.ts 신규(분량 준수·클램프·수동 경고 커버). 아울러 세션 이전부터
깨져 있던 통합테스트 정상화 - workflow(리뷰어 동의·주소 픽스처 + 누락 db목 + 검색인증 단계),
auth.signup(sms 비활성·sdk 세션 스텁·db목 보강). 최종 27/27 통과, 프로덕션 빌드 성공.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
